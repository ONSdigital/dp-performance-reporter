---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: dp-performance-reporter

outputs:
  - name: build

run:
  path: dp-performance-reporter/ci/scripts/build-bundle.sh
