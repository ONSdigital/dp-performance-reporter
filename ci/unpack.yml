---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build-bundle
  - name: dp-performance-reporter

outputs:
  - name: build
  - name: target

run:
  path: dp-performance-reporter/ci/scripts/unpack.sh
