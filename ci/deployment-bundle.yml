---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build
  - name: artifacts
  - name: dp-performance-reporter

outputs:
  - name: deployment

run:
  path: dp-performance-reporter/ci/scripts/deployment-bundle.sh
